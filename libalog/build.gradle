apply plugin: 'com.android.library'
apply plugin: 'com.novoda.bintray-release'

publish {
//    gradlew clean build bintrayUpload -PbintrayUser=itsdf07 -PbintrayKey=1c5219c87ea1a4db9edc945156def8accae2367 -PdryRun=false
//    https://bintray.com/itsdf07/maven
    userOrg = 'itsdf07'//bintray.com用户名
    groupId = 'com.itsdf07'//jcenter上的路径
    artifactId = 'lib-alog'//项目名称
    publishVersion = '2.0.0'//版本号
    desc = ''//描述，不重要
    website = 'https://github.com/itsdf07'//网站，最好有，不重要
}
android {
    compileSdkVersion 26
    buildToolsVersion "26.0.3"

    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 26
        versionCode 1
        versionName "2.0.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
